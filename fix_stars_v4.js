const fs = require('fs'); let css = fs.readFileSync('public/style.css', 'utf8'); css = css.replace(/@keyframes star-pop \\{[^}]+\\}/, '@keyframes star-pop {\\n    0% { transform: scale(0); opacity: 0; }\\n    40% { transform: scale(1.6); opacity: 1; }\\n    100% { transform: scale(1); opacity: 1; }\\n}'); css = css.replace('.anim-star-pop { transform-origin: center; transform-box: fill-box; }', '.anim-star-pop { transform-origin: center; transform-box: fill-box; opacity: 0; transform: scale(0); }'); fs.writeFileSync('public/style.css', css, 'utf8'); console.log('Fixed stars visibility');
